import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import Messages from '@/components/messages.vue'

// todo more tests
describe('Messages.vue', () => {
  it('renders a div')
  it('renders a message')
  it('renders multiple messages')
  it('renders a message with close button')
  it('clicking close removes the message')
  it('when mounted registers its self')
  it('when unmounted unregisters its self')
})
