import { expect } from 'chai'
import { shallowMount } from '@vue/test-utils'
import navigationLogin from '@/components/nav/navigation-login.vue'

// todo more tests
describe('BtnLoginLogout.vue', () => {
  it('renders a button')
  it('clicking login goes to a url')
  it('clicking logout goes to a url')
  it('makes a api call on mounting')
  it('when logged in a username should show')
  it('when logged in `isLoggedIn` should be true')
})
